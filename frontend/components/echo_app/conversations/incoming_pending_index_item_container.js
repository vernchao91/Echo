import { connect } from "react-redux";
import IncomingPendingIndexItem from "./incoming_pending_index_item";

const mapStateToProps = state => {
  return {
    
  }
}

export default connect(mapStateToProps, null)(IncomingPendingIndexItem)